# PostgresAI Monitoring stack configuration
# Copy this file to .env and adjust values as needed:
#   cp .env.example .env
#
# This file is automatically read by docker compose

# Stack version - all postgresai/* images use this tag
PGAI_VERSION=0.14.0-dev.22

# Optional: Grafana admin password (default: demo)
# GF_SECURITY_ADMIN_PASSWORD=your-secure-password

# Optional: Bind hosts for exposed ports (default: all interfaces)
# BIND_HOST=127.0.0.1:
# GRAFANA_BIND_HOST=127.0.0.1:
