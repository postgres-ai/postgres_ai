services:
  # Local development override: build metrics-server from source instead of pulling an image.
  metrics-server:
    build:
      context: ./cli
      dockerfile: Dockerfile.metrics-server
    image: postgresai/metrics-server:local


